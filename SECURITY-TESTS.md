# 인증·권한 확인 기록

- 확인 시각: 2026-09-02 17:00:38 (Asia/Seoul)
- 확인 환경: Supabase 전환 코드는 작성 완료. 실제 HTTP 재검증은 프로젝트 연결 후 수행한다.
- 계정 A/B 이메일, 비밀번호, 세션 토큰: `[가림]`

| 확인                | 성공 요청                                                  | 거절 요청                              | 결과                                    |
| ------------------- | ---------------------------------------------------------- | -------------------------------------- | --------------------------------------- |
| 1. 로그인 자격 증명 | `POST /api/auth/login` 올바른 계정 → `200`                 | 틀린 비밀번호 → `401`                  | 같은 실패 문구로 계정 존재 여부를 숨김  |
| 2. 자료 조회        | 계정 A `GET /api/experiment` → `200`                       | 비로그인 `GET /api/experiment` → `401` | 로그인 화면으로 돌아가며 자료 본문 없음 |
| 3. 자료 분리·수정   | 계정 A가 자기 기록 `PUT /api/records/{id}` → `200`         | 계정 B가 A의 id로 같은 요청 → `404`    | `WHERE id=? AND user_id=?`에서 차단     |
| 4. 자료 분리·삭제   | 계정 A가 자기 최근 기록 `DELETE /api/records/{id}` → `200` | 계정 B가 A의 id로 같은 요청 → `409`    | B의 목록·응답 본문에 A의 id나 자료 없음 |
| 5. 로그아웃 만료    | 로그아웃 전 같은 세션 조회 → `200`                         | 로그아웃 뒤 같은 세션 조회 → `401`     | 서버 세션 행 삭제 확인                  |

추가 흐름 확인: 1·2일차 저장 `201/201` → 규칙 변경 전 3일차 `409` → 규칙 변경 `200` → 3·4·5일차 `201/201/201` → 6번째 기록 `409`. 검증용 값의 손계산 합계/평균은 `355/71`이었고 화면 계산과 일치했다. JSON 내보내기 `200`, 계정 삭제 뒤 조회 `401`, T06 실제 입력 기록 이전 `200`을 확인했다.

요청·응답 로그에는 비밀번호 원문, 세션 토큰 원문, 비밀키가 없다. 테스트 중 사용한 값은 이 문서에서 모두 `[가림]` 처리했다.
