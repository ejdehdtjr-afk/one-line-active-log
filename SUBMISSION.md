# T07 제출문 — FIVE / FIVE

## 제출 주소

- 결과물: https://one-line-active-log-next.vercel.app/
- 공개 소스(검증 기록 포함 고정 commit): https://github.com/ejdehdtjr-afk/one-line-active-log/commit/57fd40582f5e95878d6efbccf2b843916549d663
- 이어서 사용한 최종 T06 commit: `e1352d1a3c32824a074072b8d52cca0dcddbf2d7`
- T06 commit 확인: https://github.com/ejdehdtjr-afk/one-line-active-log/commit/e1352d1a3c32824a074072b8d52cca0dcddbf2d7

T07은 위 T06 commit을 직접 조상으로 둔다. 같은 저장소의 T06 결과를 이어서 고쳤고 최종 결과는 Vercel에 배포했다. T06의 `daily-active-log-v2` 중 `source: user`인 실제 입력 기록만 로그인한 계정의 `legacy_records`로 옮기며, 합성 표본은 내 자료로 가장하지 않는다.

## 인증 구현 설명서

### ① 무엇으로 붙였나

별도 인증 라이브러리나 인증 서비스 없이 앱 자체 이메일 인증을 직접 구현했다. 실행 환경은 Next.js `16.3.4`와 Node.js Web Crypto API이며, 비밀번호는 `PBKDF2-SHA-256`, 210,000회 반복, 계정별 16바이트 무작위 salt로 해시한다. PBKDF2를 고른 이유는 Node.js 실행 환경에 기본 제공되는 검증된 Web Crypto 구현을 사용해 별도 네이티브 의존성 없이 반복 횟수와 계정별 salt를 명시적으로 적용할 수 있기 때문이다. 로그인 세션은 256비트 무작위 토큰을 발급하되 Supabase Postgres에는 그 토큰의 SHA-256 해시만 저장하고, 브라우저에는 `HttpOnly; Secure; SameSite=Lax` 쿠키로 전달한다. 세션 만료는 7일이다.

### ② 왜 그걸 골랐나

심사자가 별도 OAuth 계정 없이 첫 화면과 가입·로그인 흐름을 확인할 수 있고, 서버·DB에 원문 비밀번호와 원문 세션 토큰을 남기지 않기 때문이다. Supabase Postgres를 서버 전용 HTTPS API로 사용해 지속성과 관리 화면을 확보하고, 모든 테이블의 RLS를 켠 뒤 공개 역할의 직접 접근을 막았다. 앱 서버는 각 자료 요청에 `user_id`를 적용한다.

함께 검토한 대안은 외부 OAuth/SIWC와 브라우저 `localStorage` 유지다. 외부 OAuth는 심사 흐름에 제3자 인증을 추가하고, `localStorage`만으로는 서버가 계정 소유자를 확인할 수 없어 선택하지 않았다.

### 비밀번호 저장 확인

`lib/auth.ts`의 `hashPassword`를 직접 작성했고, 가입 API는 그 반환값만 `users.password_hash`에 저장한다. 실제 비밀번호 원문은 출력하거나 제출문에 적지 않았다. 같은 임시 입력을 두 번 처리해 저장 형식을 재현한 결과는 아래와 같으며, 두 값의 salt와 결과가 서로 다르고 입력 원문은 어느 값에도 포함되지 않았다.

```text
계정 A: pbkdf2-sha256$210000$Mwxx03govlrzrmyckSMOfw==$aHdYE1NsrgCHMjswOIBZOUaFC5U7tclj4bRj6980ZDs=
계정 B: pbkdf2-sha256$210000$ocsBpbx94qDkkVTXzrnrTQ==$jrpL+F1Nv0fEWG9b6l5zOnjLrFGOtsf+9MCuWOc/sSc=
결과: 같은 입력이어도 계정별 무작위 salt 때문에 저장값이 다르다.
```

로그인 요청의 비밀번호는 요청 처리 중에만 사용하고 로그에 남기지 않는다. 응답은 성공 시 `{ "ok": true }`, 실패 시 `{ "error": "이메일 또는 비밀번호가 맞지 않습니다." }`뿐이며 비밀번호나 해시를 포함하지 않는다.

### ③ 어디를 어떻게 고쳤나

- 가입: `app/api/auth/signup/route.ts` → 입력 검증 → `lib/auth.ts` 비밀번호 해시 → `users`, 기본 `experiments`, `sessions` 저장.
- 로그인: `app/api/auth/login/route.ts` → 이메일 조회 → `verifyPassword` → 새 세션과 쿠키 발급.
- 로그아웃: `app/api/auth/logout/route.ts` → 현재 토큰의 해시로 서버 세션 삭제 → 쿠키 만료.
- 다이어리 자료 조회·생성: `app/api/diary/route.ts` → `requireApiUser` → 현재 사용자의 `user_id`로 T06 다이어리 기록만 조회하거나 생성한다.
- 다이어리 자료 수정·삭제: `app/api/diary/[id]/route.ts`에서 URL의 id만 믿지 않고 `legacy_id`와 로그인 사용자의 `user_id`를 함께 건다.
- 5일 실험 자료 조회: `app/api/experiment/route.ts` → `requireApiUser` → 현재 사용자의 `user_id`로 실험·5일 기록만 조회한다.
- 5일 자료 생성·수정·삭제: `app/api/records/route.ts`, `app/api/records/[id]/route.ts`에서 모든 쿼리에 로그인 사용자의 `user_id`를 함께 건다.
- T06 이전: `app/api/migrate-t06/route.ts`에서 실제 사용자 입력만 현재 계정 소유로 저장한다. 로그인 뒤 첫 화면의 다이어리 목록에서 이전 결과를 그대로 조회·수정·삭제할 수 있다.

### ④ 안 열리는 것을 확인한 기록

`SECURITY-TESTS.md`에 성공 요청과 거절 요청을 나란히 적었다. 요약하면 중복 가입 `409`, 존재하지 않는 계정과 틀린 비밀번호의 동일 문구 `401/401`, 로그인 자료 조회 `200/401`, 양방향 타인 수정 `404/404`, 양방향 타인 삭제 `404/404`, 로그아웃 전후 같은 조회 `200/401`이다. 두 계정 모두 자료를 가진 상태에서 양쪽 목록에 상대 자료가 없고, URL query와 `X-User-Id` 헤더에 상대 계정을 넣어도 쿠키 소유자의 자료만 돌아오며, 거절 전후 자료 건수가 같음을 확인한다.

### ⑤ AI와 나

- AI에게 맡긴 일: 인증 구조 초안, Supabase Postgres 계정별 저장, 세션·권한 검사, 5일 흐름 강제, 합계·평균, 내보내기·삭제, 반복 HTTP 검증을 맡겼다.
- 내가 직접 판단한 일: T06에서 계획을 운동·ALEPH 수업·개인공부로 나누고 단위를 분으로 정한 결정, 공개 범위는 내 계정으로 제한한다는 결정, 질문·지표·예외 처리와 실제 기록 내용을 판단했다.
- AI 제안을 따르지 않은 일: 합성 기록을 실제 5일 사용 기록으로 제출하자는 방식은 따르지 않았다. 실제 5일은 내가 서로 다른 실제 날짜에 직접 입력해야 과제의 전후 비교가 의미 있기 때문이다.

### ⑥ 아직 못 막은 것

- 무차별 대입 속도 제한이 아직 없다. 공격자가 로그인 요청을 빠르게 반복해 비밀번호를 추측할 위험이 있다.
- 비밀번호 재설정이 아직 없다. 비밀번호를 잊으면 기존 계정을 되찾을 수 없다.
- 두 번째 인증 수단과 보안 감사 로그가 아직 없다. 비밀번호가 노출되면 추가 방어가 없고, 사후 추적 정보도 제한된다.

## 5일 관찰 규칙

- 답하려는 질문 한 문장: `계획한 운동 시간을 실제로 지켰는가?`
- 관찰 지표 한 개: `운동 시간`
- 단위: `분`
- 계산: Asia/Seoul 날짜별 값을 한 번씩 합산하고, 평균은 `합계 ÷ 기록일 수`로 계산한다.
- 값이 빠질 때: 기록일 수에서 제외하고 0으로 바꾸지 않는다.
- 값이 중복될 때: 같은 날짜는 DB와 API에서 거절하며, 확인된 값 하나만 남긴다.
- 값이 유난히 튈 때: 실제 측정이면 그대로 두고 메모에 이유를 적는다.
- 반올림: 합계는 정수, 평균은 소수 첫째 자리에서 반올림해 정수로 표시한다.
- 주 시작 요일: 월요일.
- 변경 전 계획 규칙: `복싱을 하루 1시간 한다.`
- 변경 후 계획 규칙: `8월 30일에는 복싱 1시간을 하고, 8월 31일부터 헬스를 하루 2시간 한다.`
- 바꾼 이유: `운동 종목을 복싱에서 헬스로 전환하고 운동 시간을 늘리기 위해 계획을 조정했다.`

앱은 정확히 2일이 저장된 때에만 계획 규칙 변경을 허용하고 서버 시각과 이유를 함께 저장한다. 변경 전 2일과 변경 후 3일은 같은 질문·지표·단위·계산 규칙을 사용한다. 3일차를 먼저 저장하려 하면 `409`, 5일 뒤 추가하려 해도 `409`를 돌려준다. 5일이 차면 화면에 각 값을 더한 식과 `합계 ÷ 5` 식을 같이 보여 화면 합계·평균과 손계산을 대조한다.

실제 활동 자료는 사용자가 수행한 내용을 바탕으로 계정 안에 직접 기록했다. 활동일과 값은 `2026-08-28 60분(복싱)`, `08-29 60분(복싱)`, `08-30 60분(복싱)`, `08-31 120분(헬스)`, `09-01 120분(헬스)`이다. 앱에는 이 다섯 날짜만 5일 실험으로 고정되어 있다. 활동은 해당 날짜에 실제로 했지만 앱 입력은 2026-09-04에 한 번에 정리했으며, 입력 시각을 과거로 조작하지 않았다.

손계산은 `60 + 60 + 60 + 120 + 120 = 420분`, `420 ÷ 5 = 84분`이다. 변경 전 평균은 `(60 + 60) ÷ 2 = 60분`, 변경 후 평균은 `(60 + 120 + 120) ÷ 3 = 100분`이며 화면 값과 같다.

## 내보내기와 삭제

`내 계정`에서 JSON 한 파일로 계정 메타데이터, T06 이전 기록, 질문·계산 규칙·규칙 변경·5일 기록을 내보낸다. 비밀번호 해시와 세션은 내보내지 않는다. 같은 화면에 `계정을 지우면 기록, 실험 설정, 로그인 세션이 모두 함께 영구 삭제됩니다`라고 적었고, `계정 삭제`를 직접 입력해야 삭제된다.

## 짧은 확인 방법

### ① 어디로 가나요

결과물 주소의 `/`로 간다. 새 시크릿 창에서도 로그인 화면은 열린다.

### ② 세 단계 안에 무엇을 하나요

① `/`에서 로그인 화면 확인 → ② 새 계정을 만들거나 로그인 → ③ `오늘의 한 줄 기록`과 `5일 실험` 메뉴 확인.

### ③ 무엇이 보이면 통과인가요

로그인하지 않으면 기록이 보이지 않고, 로그인 뒤 내 질문·합계·평균·내 기록만 보이며 2일차 뒤에는 규칙 변경 상자가 보이면 통과다.

### ④ 안 될 때는 무엇이 보이나요

비로그인 자료 요청은 `로그인이 필요합니다(401)`, 타인 기록은 `내 기록에서 찾을 수 없습니다(404)` 또는 순서 보호 `409`, 3일차를 너무 일찍 저장하면 규칙을 먼저 바꾸라는 안내가 보인다.

## 비밀값 점검

공개 화면·소스·제출문에는 비밀번호, 원문 세션 토큰, API 키, 비밀키가 없다. 예시가 필요한 곳은 `[가림]`만 사용했다. 저장소의 실제 환경 값 파일은 만들지 않았다.
