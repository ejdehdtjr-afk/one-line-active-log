# T07 제출문 — FIVE / FIVE

## 제출 주소

- 결과물: https://one-line-active-log-next.vercel.app/
- 공개 소스: https://github.com/ejdehdtjr-afk/one-line-active-log
- 이어서 사용한 최종 T06 commit: `e1352d1a3c32824a074072b8d52cca0dcddbf2d7`
- T06 commit 확인: https://github.com/ejdehdtjr-afk/one-line-active-log/commit/e1352d1a3c32824a074072b8d52cca0dcddbf2d7

T07은 위 T06 commit을 직접 조상으로 둔다. 같은 저장소의 T06 결과를 이어서 고쳤고 최종 결과는 Vercel에 배포했다. T06의 `daily-active-log-v2` 중 `source: user`인 실제 입력 기록만 로그인한 계정의 `legacy_records`로 옮기며, 합성 표본은 내 자료로 가장하지 않는다.

## 인증 구현 설명서

### ① 무엇으로 붙였나

앱 자체 이메일 가입·로그인을 붙였다. 비밀번호는 Web Crypto의 `PBKDF2-SHA-256`, 210,000회 반복, 계정별 16바이트 무작위 salt로 해시한다. 로그인 세션은 256비트 무작위 토큰을 발급하되 Supabase Postgres에는 그 토큰의 SHA-256 해시만 저장하고, 브라우저에는 `HttpOnly; Secure; SameSite=Lax` 쿠키로 전달한다. 세션 만료는 7일이다.

### ② 왜 그걸 골랐나

심사자가 별도 OAuth 계정 없이 첫 화면과 가입·로그인 흐름을 확인할 수 있고, 서버·DB에 원문 비밀번호와 원문 세션 토큰을 남기지 않기 때문이다. Supabase Postgres를 서버 전용 HTTPS API로 사용해 지속성과 관리 화면을 확보하고, 모든 테이블의 RLS를 켠 뒤 공개 역할의 직접 접근을 막았다. 앱 서버는 각 자료 요청에 `user_id`를 적용한다.

함께 검토한 대안은 외부 OAuth/SIWC와 브라우저 `localStorage` 유지다. 외부 OAuth는 심사 흐름에 제3자 인증을 추가하고, `localStorage`만으로는 서버가 계정 소유자를 확인할 수 없어 선택하지 않았다.

### ③ 어디를 어떻게 고쳤나

- 가입: `app/api/auth/signup/route.ts` → 입력 검증 → `lib/auth.ts` 비밀번호 해시 → `users`, 기본 `experiments`, `sessions` 저장.
- 로그인: `app/api/auth/login/route.ts` → 이메일 조회 → `verifyPassword` → 새 세션과 쿠키 발급.
- 로그아웃: `app/api/auth/logout/route.ts` → 현재 토큰의 해시로 서버 세션 삭제 → 쿠키 만료.
- 자료 조회: `app/api/experiment/route.ts` → `requireApiUser` → 현재 사용자의 `user_id`로 실험·5일 기록·T06 이전 기록만 조회.
- 자료 생성·수정·삭제: `app/api/records/route.ts`, `app/api/records/[id]/route.ts`에서 모든 쿼리에 로그인 사용자의 `user_id`를 함께 건다. URL의 id만으로 접근하지 않는다.
- T06 이전: `app/api/migrate-t06/route.ts`에서 실제 사용자 입력만 현재 계정 소유로 저장한다.

### ④ 안 열리는 것을 확인한 기록

`SECURITY-TESTS.md`에 다섯 쌍의 성공 요청과 거절 요청을 나란히 적었다. 요약하면 로그인 자격 증명 `200/401`, 로그인 자료 조회 `200/401`, 자기/타인 수정 `200/404`, 자기/타인 삭제 `200/409`, 로그아웃 전후 조회 `200/401`이다. 계정 B의 목록과 거절 본문에 계정 A의 자료 id가 섞이지 않았다.

### ⑤ AI와 나

- AI에게 맡긴 일: 인증 구조 초안, Supabase Postgres 계정별 저장, 세션·권한 검사, 5일 흐름 강제, 합계·평균, 내보내기·삭제, 반복 HTTP 검증을 맡겼다.
- 내가 직접 판단한 일: T06에서 계획을 운동·ALEPH 수업·개인공부로 나누고 단위를 분으로 정한 결정, 공개 범위는 내 계정으로 제한한다는 결정, 질문·지표·예외 처리와 실제 기록 내용을 판단했다.
- AI 제안을 따르지 않은 일: 합성 기록을 실제 5일 사용 기록으로 제출하자는 방식은 따르지 않았다. 실제 5일은 내가 서로 다른 실제 날짜에 직접 입력해야 과제의 전후 비교가 의미 있기 때문이다.

### ⑥ 아직 못 막은 것

- 무차별 대입 속도 제한이 아직 없다. 공격자가 로그인 요청을 빠르게 반복해 비밀번호를 추측할 위험이 있다.
- 비밀번호 재설정이 아직 없다. 비밀번호를 잊으면 기존 계정을 되찾을 수 없다.
- 두 번째 인증 수단과 보안 감사 로그가 아직 없다. 비밀번호가 노출되면 추가 방어가 없고, 사후 추적 정보도 제한된다.

## 5일 관찰 규칙

- 답하려는 질문 한 문장: `계획한 집중 시간을 실제로 지켰는가?`
- 관찰 지표 한 개: `집중 시간`
- 단위: `분`
- 계산: Asia/Seoul 날짜별 값을 한 번씩 합산하고, 평균은 `합계 ÷ 기록일 수`로 계산한다.
- 값이 빠질 때: 기록일 수에서 제외하고 0으로 바꾸지 않는다.
- 값이 중복될 때: 같은 날짜는 DB와 API에서 거절하며, 확인된 값 하나만 남긴다.
- 값이 유난히 튈 때: 실제 측정이면 그대로 두고 메모에 이유를 적는다.
- 반올림: 합계는 정수, 평균은 소수 첫째 자리에서 반올림해 정수로 표시한다.
- 주 시작 요일: 월요일.
- 변경 전 계획 규칙: `집중 작업을 오전 10시에 시작한다.`

앱은 정확히 2일이 저장된 때에만 계획 규칙 변경을 허용하고 서버 시각과 이유를 함께 저장한다. 변경 전 2일과 변경 후 3일은 같은 질문·지표·단위·계산 규칙을 사용한다. 3일차를 먼저 저장하려 하면 `409`, 5일 뒤 추가하려 해도 `409`를 돌려준다. 5일이 차면 화면에 각 값을 더한 식과 `합계 ÷ 5` 식을 같이 보여 화면 합계·평균과 손계산을 대조한다.

실제 5일 값과 실제 규칙 변경 문장은 사용자가 앱에서 직접 기록한다. 이 제출문에는 실제 사용을 가장한 합성 값을 적지 않는다.

## 내보내기와 삭제

`내 계정`에서 JSON 한 파일로 계정 메타데이터, T06 이전 기록, 질문·계산 규칙·규칙 변경·5일 기록을 내보낸다. 비밀번호 해시와 세션은 내보내지 않는다. 같은 화면에 `계정을 지우면 기록, 실험 설정, 로그인 세션이 모두 함께 영구 삭제됩니다`라고 적었고, `계정 삭제`를 직접 입력해야 삭제된다.

## 짧은 확인 방법

### ① 어디로 가나요

결과물 주소의 `/`로 간다. 새 시크릿 창에서도 로그인 화면은 열린다.

### ② 세 단계 안에 무엇을 하나요

`계정 만들기` → 이메일·8자 이상 비밀번호 입력 → 로그인 뒤 `5일 기록` 화면 확인.

### ③ 무엇이 보이면 통과인가요

내 질문, 합계·평균 카드, 내 기록만 보이고 2일차 뒤에는 규칙 변경 상자가 보이면 통과다.

### ④ 안 될 때는 무엇이 보이나요

비로그인 자료 요청은 `로그인이 필요합니다(401)`, 타인 기록은 `내 기록에서 찾을 수 없습니다(404)` 또는 순서 보호 `409`, 3일차를 너무 일찍 저장하면 규칙을 먼저 바꾸라는 안내가 보인다.

## 비밀값 점검

공개 화면·소스·제출문에는 비밀번호, 원문 세션 토큰, API 키, 비밀키가 없다. 예시가 필요한 곳은 `[가림]`만 사용했다. 저장소의 실제 환경 값 파일은 만들지 않았다.
